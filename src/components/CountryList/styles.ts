import { StyleSheet } from 'react-native';

export const Constants = {
  defaultPaddingBottom: 28,
};

const styles = StyleSheet.create({
  contentContainer: {
    paddingTop: 28,
  },
  itemSeparator: {
    marginVertical: 8,
  },
});

export default styles;
